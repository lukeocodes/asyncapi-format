asyncapi: 2.6.0
info:
  title: Test Bundle API
  version: 1.0.0
channels:
  user/created:
    subscribe:
      message:
        payload:
          $ref: "./components/schemas/User.yaml"
components:
  schemas:
    User:
      $ref: "./components/schemas/User.yaml"
